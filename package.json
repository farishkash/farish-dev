{
  "name": "farish-dev",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.7",
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
